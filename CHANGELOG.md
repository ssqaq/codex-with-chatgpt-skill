# 更新记录

## 1.19.2（2026-09-12）

1. 修复新评审继承旧任务“恢复次数已用完”的问题；原对话继续复用，旧恢复审计保留。
2. 已受影响的旧版记录可以在明确安全的条件下恢复；同一任务反复调用不会重置次数，未知发送仍被拦住。
3. 用户允许时，原对话确实不存在可新建并携带已确认摘要；增加当前浏览器工具的丢失证据支持，不把登录或工具超时当成丢失。
4. 清理两份配套 Skill 中冲突的旧浏览器接口、恢复计数和标签丢失说明，统一按当前运行规则恢复。
5. 补测真实4轮及3轮两组方案评审、实际执行、测试、页面和最终复核；详细结果与未全部达标的检查间隔见 docs/verification/1.19.2.md。

## 1.19.1（2026-09-12）

1. 修复临时冻结、带等待记录时无法进入原浏览器恢复的问题，最多一次、限时60秒。
2. 使用当前宿主公开的 CUA 浏览器入口，兼容确实存在的旧入口。
3. 增加本轮完整回复校验、显式轮次读取示例，避免短回复或旧消息造成假等待。
4. 记录各阶段真实时间，修复回执覆盖发送前核验时间、日期毫秒和时区比较问题。
5. 补充独立最终复核阶段与完成门槛；保留原执行起点，取消、分歧、检查失败不能完成。
6. 补测真实3轮方案评审、独立计时项目修改、测试、页面和最终复核。详细过程与速度限制见 docs/verification/1.19.1.md。


## 1.19.0（2026-09-12）

这次主要是让多轮评审更快、更不容易看起来像卡死：

1. 从第 2 轮开始只发送本轮新增的事实、修改点和还没解决的分歧，不再把上一轮整段方案重复发一遍。
2. 把两种计时分开：等待 DeepSeek 时显示“等待网页回复”，Codex 改代码时显示“执行中”以及已经用了多少分钟。
3. 连续 10 分钟没有确认到新的回复内容，自动暂停并告诉你检查哪里；网页正常思考时不会因为两次画面没变化就误判卡死。
4. 遇到 429（请求太多）或 503（服务暂时不可用）会按次数逐步增加等待时间，保存下次检查时间，不重复发送、不重复建会话。
5. 双方达成共识后立即记录普通执行任务接力请求；如果原任务只能出方案，不再等你再次发送“继续”。
6. 增加 503、后续轮次增量、执行计时和自动接力的测试。

## 1.18.3（2026-09-12）

1. 修复根目录 Skill 和核心 Skill 的说明文字不一致，避免 GitHub 自动检查误报。
2. 保留 1.18.2 的多轮评审限流保护和共识后自动执行修复。

## 1.18.2（2026-09-12）

1. 多轮评审遇到 429 限流时会明确暂停，不再无限等待、重复发送或重复创建会话。
2. 保存限流发生时间、次数和可恢复时间；只有显式恢复检查且时间已到，才尝试一次。
3. 限流状态不保存服务返回原文，避免把令牌或账号信息写入本地记录。
4. 增加 429 异常、429 返回值、恢复时间和敏感信息过滤测试。

## 1.18.1（2026-09-12）

1. 修复进入下一轮时漏传“唯一标签数量”导致无法取得浏览器使用权的问题；只在任务、标签和浏览器编号完全一致时自动补齐。
2. 修复释放浏览器使用权时漏传重复的页面来源字段导致误报的问题；字段写错或身份不一致仍然拒绝。
3. 修复多轮评审已经达成共识，却因为当前任务只能出方案而不继续改代码的问题。现在会记录自动接力状态，在同一工作区建立普通执行任务，直接修改、测试和复核，不需要用户再发“继续”。
4. 增加反向测试：错误任务、错误标签、错误浏览器编号和错误页面来源都不能借自动补值接管会话。

## 1.18.0（2026-09-12）

1. 补齐 DeepSeek 单次和多轮配套安装文件，安装前后校验文件和版本，并保留上一版备份。
2. 分开“网页正常思考”和“本地发送失败”，取消正常等待两次检查或一分钟就暂停的冲突规则。
3. 发送前必须提供内置浏览器刚读取的页面证据，核对原会话、输入框、开关和消息；不再硬填“输入框可用”。
4. 保存每轮等待起点和实际页面检查时间，每满一分钟生成新进度，重启后继续计时；未检查时明确说明无法确认网页状态。
5. 增加当前任务的检查点保存和恢复；保存失败不再静默忽略，恢复后重新核对原网页和回执，再按原门槛继续。
6. 增加模拟故障和真实本地脚本流程测试，覆盖全新安装、记录损坏、重启、重复消息和等待回显。发布改为等待同一提交的自动测试全部通过。

说明：进度回显依赖 Codex 正在运行任务；网页观察、发送回执和双方共识是不同步骤，不能互相代替。

## 1.17.0（2026-09-11）

- 一轮一条命令：新增 `send_review_round.ps1`，每轮发送前的指纹计算、绑定检查、发送确认合并成一条命令，缺什么直接报"缺什么、怎么补"。
- 回执自动补记：消息已到页面但本地没记账时，20–30 秒内自动补记，不用等卡住才救。
- 浏览器标签自动认路：标签编号变化时按会话 URL + 标题 + 暗号自动识别并重绑，不用人手动操作。
- 每轮留底账：每轮完成写一行 JSONL 流水账，状态文件坏了能从流水账恢复。
- 分歧进度条：每轮显示"已解决 X/Y 项分歧"，共识达成时显示全部分歧已解决。
- 恢复报位置：重启或恢复时显示"从第 N 轮继续，上一轮卡在哪一步"。
- 暂停时给对照选项：连续两轮同一分歧暂停时，把双方方案摆成对照表，回复 A/B/C 即可裁决。

## 1.16.2 - 2026-09-11

- 每轮 DeepSeek 评审消息正文末尾必须附绑定暗号，避免回执因 marker 不一致被拒。
- 增加 auditRisk 卡住时的自救路径：终止带病任务、Claim 复用同一会话、带暗号重发新一轮。
- 恢复任务时从绑定状态读取原暗号，不重新编造。

## 1.16.1 - 2026-09-11

- DeepSeek 多轮评审提速：已激活任务恢复时不再重读全部文档和脚本目录，直接读取评审状态。
- 回执自救：消息已真实发出但本地回执未确认时，立即回读页面 DOM 并补记回执，不再原地等待或重发。
- 回执记录顺序规则：`RecordSendOutcome` 因绑定未完成被拒时，先完成绑定再用原 fingerprint 重试，不重新发送。
- 每轮只调用一次 advance；本地状态无变化时不重复调用。
- 等待 DeepSeek 深度思考期间显示“正在等待思考”，两次检查后仍无新状态则暂停并说明卡点。

## 1.16.0 - 2026-09-11

- 增加 DeepSeek 默认评审渠道，支持单次和多轮共识评审。
- 支持用“用 GPT/ChatGPT 评审”切回现有 ChatGPT 流程。
- 适配 DeepSeek 官网模式合并，要求深度思考和智能搜索都保持开启。
- DeepSeek 只接收短摘要，不发送完整源码、完整 diff 或日志。
- 增加渠道、任务、轮次、回执和执行门槛的本地状态与命令。

## 1.15.5 - 2026-09-11

- 增加连接器、浏览器授权、502/530 和 Codex/Work 额度异常的快速暂停保护。
- 多轮评审最多等待约 1 分钟，恢复时从保存的轮次继续，不重复发送或重开聊天。
- 增加 `Codex auth token is unavailable` 等常见卡住原因的中文说明。

## 1.15.4 - 2026-09-11

- 增加“多轮方案评审后修改代码”的完整中文调用模板。
- 同时支持“多轮评审”和“多轮复审后再修改代码”等常用说法。
- 同步根 Skill、核心 Skill、本地安装版和中英文使用说明。

## 1.15.3 - 2026-09-10

- 计划模式相关用户提示统一改成“计划修改的方案内容”“只做计划方案”“修改方案正常执行”。

## 1.15.2 - 2026-09-10

- 用户可见标题统一改成大白话中文，内部协议字段保持英文不变。

## 1.15.1 - 2026-09-10

- 增加计划模式切换失败的硬停止保护。
- 客户端拒绝创建普通任务或切换模式时立即标记阻塞，不再无限等待或重复发送。

## 1.15.0 - 2026-09-10

- 普通任务不再依赖 Codex 顶部计划/执行模式。
- 检测到原始 `<proposed_plan>` 或连续计划模式等待时，自动转入普通执行续接。
- 计划模式恢复保留原工作区、连接器和历史，不修改 Codex 数据库或旧消息。

## 1.14.1 - 2026-09-10

- 统一离线版本检查的中文提示，明确显示“是否需要更新：无法确认”。

## 1.14.0 - 2026-09-10

- Windows 安装脚本在 winget 安装后自动刷新当前 PowerShell 的 PATH。
- 损坏的旧会话会保留备份并明确报错，不会静默创建新会话。
- 离线版本检查显示“未确认最新”，不再把无法连接 GitHub 当成已是最新。
- 普通任务增加分析、规划、修改、测试、复核和页面检查阶段回显。
- `c2c record` 强制要求成功测试、自检、页面验证和验证时间证据。
- 更新安全和架构文档中的旧版本表述。

## 1.13.0 - 2026-09-10

- 修正公开文档中的主仓库地址、Skill 安装路径和首次配置说明。
- 增加图片传输和额度边界说明，以及实际模型不可强制切换的说明。
- 增加可保存的修改后自检、页面验证和同步闸门；未通过验证不能完成执行记录。
- 加固 OAuth scope、待处理授权请求、代理头、配对限流和固定连接启动检查。
- 加固备份/回滚的自定义路径、原子写入和冲突命名，并同步安装参考文件和界面元数据。
- 拆分 Skill 详细规则到 references，并增加公开文档一致性 CI 检查。

## 1.12.0 - 2026-09-09

- 增加强制的修改后自检、代码复核和页面验证闸门。
- 修改影响页面时，要求只用内置 ChatGPT 浏览器检查加载、主要入口和刷新/重新打开。
- 任一检查失败时禁止发送 `EXECUTED`、提交、推送或同步；通过后回显三项验证结果。
- `EXECUTED` 协议消息增加 `SELF_CHECK`、`PAGE_VERIFY` 和 `PAGE_SCOPE` 证据字段。

## 1.11.0 - 2026-09-09

- 图片读取增加真实结构和尺寸校验，限制宽高、总像素数和并发读取数量。
- 增加仅限 `codex-clipboard-*` 临时截图的显式只读授权，不开放任意外部路径。
- 图片中的文字明确按不可信资料处理，不作为操作指令。
- 更新前检测未提交改动并停止，回滚默认拒绝覆盖脏工作区。
- 版本检查增加 GitHub 最新 Release 版本字段和三行中文结果。

## 1.10.0 - 2026-09-09

- 增加只读 `read_image` MCP 工具，支持 PNG、JPG/JPEG、WEBP 和 GIF。
- 图片限制在工作区内且单张不超过 10 MB，校验文件头并沿用敏感文件拦截。
- 图片通过当前连接临时返回，不写入 ChatGPT 文件区，不调用外部视觉服务。
- 图片读取可接入快速流程和纯文字多轮方案评审，失败时降级为纯文字流程。
- 同步更新 Skill、协议、安全说明和桥接服务版本。

## 1.9.0 - 2026-09-09

- 增加纯文字多轮方案评审和 `CONSENSUS_PLAN` / `CONSENSUS_REVIEW` / `CONSENSUS` 协议状态。
- 每轮在 Codex 中显示当前轮数，支持中断后从保存的轮次继续。
- 共识确认前禁止修改文件；图片读取能力仍未启用。

## 1.8.0 - 2026-09-09

- 增加纯文字多轮方案评审：Codex 与网页版 ChatGPT 反复讨论，双方确认 `CONSENSUS` 后才修改。
- 每轮回显当前轮数，支持旧会话中断后从保存的轮次恢复。
- 连续两轮出现相同分歧时暂停并等待用户处理。
- 暂不包含图片直接读取，图片能力留到纯文字流程验证通过后单独实现。

## 1.7.0 - 2026-09-09

- 增加更新前自动备份、失败自动回滚，以及手动备份/回滚脚本。
- 版本检查统一显示本机版本、GitHub 最新版本和是否需要更新。
- 同步旧会话前显示会话名称和工作区，降低发错会话的风险。

## 1.6.1 - 2026-09-09

- 修正更新步骤的 Skill 文件路径：优先使用仓库根目录 `SKILL.md`，兼容旧版 `core/skill/SKILL.md`。

## 1.6.0 - 2026-09-09

- 增加已打开旧会话的 Skill 同步流程：继续使用原会话、原连接器，不新建、不重复配对。
- 说明历史消息不会被改写，只有后续回合读取新规则。

## 1.5.0 - 2026-09-09

- 增加 GPT-5.6 Sol + Pro 最高强度的推荐和真实可用性检查说明。
- 明确模型不可见时使用实际列表里的最高模型，不伪造模型名称。

## 1.4.0 - 2026-09-09

- 增加 GitHub 自动测试工作流。
- 增加根据 `VERSION` 自动创建标签和 Release 的工作流。
- 修正自动测试使用的 Node.js 版本，兼容当前 pnpm。

## 1.3.0 - 2026-09-09

- 合并完整程序源码到 `core/`，以后本仓库作为唯一更新入口。
- 一键安装脚本改为从本仓库获取最新版本。
- 修复换项目后反复询问连接方式的问题已同步到 `core/`。

## 1.2.0 - 2026-09-09

- 修复换项目后反复询问连接方式的问题：自动沿用本机最近一次选择。
- 增加自动更新脚本、卸载脚本和版本检查脚本。
- 补充中文使用说明。

## 1.1.0 - 2026-09-09

- 增加中文大白话安装说明和三张示意图。
- 统一连接器名称为 `Codex with ChatGPT · <项目名>`。
- 增加 Windows PowerShell 和 macOS/Linux 一键安装脚本。
- 增加版本文件和 GitHub Release。
