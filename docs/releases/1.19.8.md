# 1.19.8：补上关闭标签和发送态的可验证口径

第2轮评审指出关闭标签恢复和发送态复位还缺可验证口径，本版按第3、4轮共识补上：

1. **关闭标签恢复**：not-found 来自 getTab 清单缺失 oldTabId，并核验会话号、绑定暗号；同一关闭标签指纹最多恢复一次。
2. **发送态复位**：sendOwner 为空时暂停人工核对；ForceTerminate 遇到未确认回执或审计风险时拒绝，避免误清在途发送。
3. **真实复测**：同一官方会话完成 4 轮，第3轮和第4轮均为 CONSENSUS，期间没有新的发送链路故障。

本机复核结果：

- 28 个测试文件、509 项测试全部通过。
- 类型检查通过；构建通过。
- 两份 DeepSeek Skill 已同步安装并校验为 1.19.8。