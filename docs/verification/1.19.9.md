# 1.19.9 检查记录

核对日期：2026年9月15日。此文件记录实际检查结果，不是正式发布公告。

## 1. 当前结论

1. 本机自动化检查已通过。
2. 当前浏览器控制授权不可用，修复后新的真实网页3—4轮评审尚未完成。
3. 不能认定整个功能全部正常。用户在了解本地检查已通过、真实网页复测尚未完成后，明确确认上传并创建正式 Release；本次按此范围发布，发布记录必须保留网页复测未完成的说明。
4. 全局 Skill 先前已显示1.19.9，但文件内容落后于本次候选。版本号相同不等于文件相同，不能只看版本号声称同步完成。

## 2. 本次修复

1. 重复确认同一发送回执时，保留第一次发送和收到回执的时间，只更新最近检查时间。
2. JSON 读取保留原始时间字符串，避免 PowerShell 自动转换时间后删掉小数末尾的零；旧记录没有首次时间时仍显示未记录。
3. 诊断报告按同一时间点处理 UTC 和本地时区，避免多算八小时。 旧版解析路径也保留日期原文及空数组、单元素和嵌套数组结构，不把数组变成额外的包装对象。
4. 读取器支持当前公开浏览器接口的显式回调。没有可用接口或未验证的页面读取适配时明确暂停，不假定旧接口存在。
5. 拒绝把旧缓存当新观察；超过30秒保留真正读到的回复和时间，但判定速度验收未通过，不因此重复发送。
6. 新增原始观察验收器，重新计算首次检查延迟和相邻检查间隔，不接受自报的“通过”或共识文字代替时间证据。

## 3. 本机复测

| 序号 | 检查内容 | 实际结果 |
|---|---|---|
| 1 | 全量测试 | 30个测试文件，590项全部通过 |
| 2 | 类型检查 | 通过 |
| 3 | 构建 | 通过 |
| 4 | 仓库内受版本管理的 PowerShell 脚本语法 | 35份通过，含核心测试夹具 |
| 5 | Bash 脚本语法 | 6份通过 |
| 6 | 观察验收器及3份读取器 JavaScript 语法 | 4份通过 |
| 7 | 配套清单、隔离安装及安装后完整性 | 通过，没有替换全局安装 |
| 8 | 两份主 Skill 入口文件一致性 | 通过 |
| 9 | 改动空白和补丁检查 | `git diff --check` 通过 |

最终全量检查执行于22:51:47—22:52:58（上海时区），测试命令耗时约67.51秒。

最终全量命令：

```text
npm.cmd --prefix core run test
npm.cmd --prefix core run typecheck
npm.cmd --prefix core run build
```

原先578项中有1项失败：时间字符串`.0173840+08:00`被自动改为`.017384+08:00`。已补固定尾零样例，先准确复现失败，再修复并重新跑完整套测试；失败日志保留，没有放松为仅比较时间点。

兼容分支另有12项回归，已计入最终590项：实际使用当前PowerShell 7.6和Windows PowerShell 5.1，并通过隐藏`DateKind`参数受控模拟旧Core解析路径；没有安装或运行真实PowerShell 7.0—7.4二进制，因此不把模拟结果说成这些版本的端到端实测。

独立复核发现的“旧Core仍会改写时间”和“Windows PowerShell 5.1根数组附带包装”两项问题均已修复并复核。最终四份脚本的3类根数组在实际Windows PowerShell 5.1中独立检查，共12次通过。该结果不替代真实网页评审。

## 4. 真实网页证据与阻塞

重新核算上一组原始记录：

| 序号 | 原轮次 | 提交至观察到完整回复 | 最大实际检查间隔 | 时间验收 |
|---|---|---|---|---|
| 1 | 第1轮 | 22.691秒 | 22.590秒 | 通过 |
| 2 | 第2轮 | 28.143秒 | 28.069秒 | 通过 |
| 3 | 第3轮 | 55.618秒 | 55.554秒 | 未通过，检查间隔超过30秒 |
| 4 | 第4轮 | 未记录 | 未记录 | 未发送，不能算通过 |

1. 上表是上一组观察记录，不是本次修复后新跑的一组；历史原样保留。
2. “55.618秒观察到完成”不能证明网页实际生成恰好用了55.618秒。真实生成结束时刻未记录。
3. 当前实际浏览器工具返回`Codex auth token is unavailable`，本机`codex login status`退出码为1，返回`401 Unauthorized`。
4. 原 DeepSeek 对话和已确认回执保留。授权失败不计为 DeepSeek 生成等待，不自动重发，不改认证配置，不用其他浏览器替代。

## 5. 还需完成

1. 恢复 Codex 登录授权，再核验内置浏览器和原官方对话。
2. 用实际公开接口验证当前页面读取适配，将观察和回复同步到核心`review observe`及`review reply`，不能只写轮次记录。
3. 从第1轮重新完成3—4轮真实方案评审；每轮保留原始观察、回显意见、验收不超过30秒的实际检查间隔。发现问题先修，再重新完整复测。
4. 验证双方共识前不改测试项目，共识后实际修改、测试、页面检查和最终复核。
5. 本次按用户明确确认提交、推送，并在远端自动测试通过后创建正式 Release。此次发布不代表真实网页已通过，也不放宽以后任务的完整验收门槛；全局 Skill 未在本次发布步骤中替换。
