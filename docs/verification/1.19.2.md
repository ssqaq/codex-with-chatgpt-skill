# 1.19.2 复测记录

## 结论

1. 新增修复已通过本机功能测试：新评审不再继承旧任务已用完的恢复次数；安全迁移旧版休眠记录；原对话确实不存在且用户已授权时允许新建并携带已确认摘要。
2. 真实 DeepSeek 完成两组完整流程：第一组 **4轮方案 + 1轮最终复核**，第二组 **3轮方案 + 1轮最终复核**。两组都在共识后真正修改独立计时项目，运行23项测试并检查页面。
3. 第二组三轮的“页面核验到提交”分别为22.309、19.390、21.155秒，均在30秒目标内。没有重现旧恢复次数、旧轮回复、重复发送或发送锁卡住。
4. **当前标准已调整**：用户确认把网页检查标准从5/15秒调整为30秒以内，首次仍立即检查。新调度按上次实际观察时间计算截止，处理时间计入，超过上限如实记毫秒。该目标受宿主调度影响，不是严格实时保证，也不是要求 DeepSeek 在30秒内生成完毕。
5. 历史复测仍按当时口径保留：第一组前两次提交超过30秒，第二组有一次检查间隔30.064秒，比新上限也多64毫秒，不能取整称为通过。新标准和新增测试随1.19.2发布；不再以已取消的5秒标准阻止发布。网页复测使用内置浏览器，深度思考和智能搜索均实际开启。

## 实际修复

1. 新 Task Claim 将旧恢复次数和状态移入审计，新任务单独计数；同一任务重复 Claim 不会清零已用预算。
2. 已受旧版影响的休眠记录，仅当前任务尚未开始浏览器/发送动作、前任务确已终态且无未知发送风险时迁移。未知前任务、当前浏览器失败和待回执均保留原状态。
3. 当前浏览器的“找不回原对话”证据不再依赖宿主不存在的旧接口。成功清单确认无可复用对话，访问原地址明确不存在，至少两次真实观察，才允许 MarkLost；随后仍复用原有替换与发送保护。
4. 清理两份配套 Skill 正文与新运行规则冲突的旧接口、按 thread 累计次数及仅因标签丢失就新建的说法。默认渠道仍是 DeepSeek。
5. 原标签关闭后找回原官方对话已经真实验证。原对话确实不存在后的新建分支是**合成证据脚本测试**，没有删除真实对话来制造现场，不能称作该分支的真实网页验收。

## 自动化和人工检查

| 序号 | 检查内容 | 结果 |
|---|---|---|
| 1 | 核心全量测试 | 30秒标准调整后22个测试文件、402项通过；前两组流程测试时为395项 |
| 2 | 可靠性专项 | 36项通过，包含真实 PowerShell 入口和合成浏览器证据 |
| 3 | 类型检查、构建 | 通过 |
| 4 | PowerShell语法、Bash语法 | 35份PowerShell脚本及安装/更新/备份等Bash脚本通过 |
| 5 | 独立项目 | 两个副本分别23项通过，原14项回归及新增9项观察时间边界 |
| 6 | 共识前保护 | 两组在 execute 前均核对基线文件哈希，未改变 |
| 7 | 共识后执行 | 实际修改计时模块和页面，不用状态写入冒充修改 |
| 8 | 页面检查 | 检查时间、暂停重开、恢复、完成、取消、新轮重置均通过 |
| 9 | 包完整性 | 隔离安装及校验通过；正式本机安装另有本地回执 |
| 10 | 独立复核 | 两组最终回复均明确 CONSENSUS；DeepSeek仅评摘要，没有本地源码或测试连接 |

页面具体检查：检查按钮不改变内容进展或等待累计；30秒内显示最近检查时间，超过30秒提示无法确认但不自行取消；缺失、非法、未来时间不补造；暂停/完成/取消时禁用；重开保留记录，新轮清空。第二组暂停关闭并重开后，持久记录仍为566毫秒及原检查时间，完整JSON相同。

## 30秒标准调整后的补测（2026-09-12）

1. 用户确认30秒以内的新标准后，新增29.999/30/30.001/45秒边界及重启保持原截止测试。调整后核心全量402项通过，类型检查、构建通过；本机主Skill和两份配套Skill均备份更新，文件核验通过。
2. 首次临时测试脚本误把single与consensus配套Skill混用，正式归属校验正确阻止同步；保留失败记录并终止该测试，没有重发已确认消息。其28.996秒网页观察仅作为一次观察证据，不算该测试完整通过。
3. 改用匹配的单次入口后，发现独立评审包激活/状态脚本遗漏智能搜索字段，导致开始时被拦截。同步现有多轮包的激活、状态和网页冒烟校验，并将实际脚本回归扩展到两个包。重新全量测试后继续原未发送测试，无须新建官方对话。
4. 修复后的真实单次发布复核：页面核验到提交20.265秒；提交后54毫秒完成第一次检查；下一次检查在24.560秒后读到完整回复。提交到完整回复观察共24.614秒，符合本次30秒检查间隔标准。
5. 回复携带正确任务与轮次，经核心校验后仍不允许执行；Codex逐项采纳、记录双方意见并同步后才进入READY。配套独立评审正常完成。这是一次发布结果复核，不能冒称又做了3–4轮方案或再次修改独立项目。
6. 本次任务从准备到提交包含修复单次包和重新测试，耗时333.715秒；不能把这段修复时间归到网页生成。该过程也不能称作全程无故障。修复后的等待检查和回显链通过。
7. 原两组完整流程及5/15秒计时数据保持不变；本次结果单独保存于[30秒补测摘要](1.19.2-check30.json)。超过30秒按真实毫秒计，不取整抹去超时，不承诺宿主严格实时。

## 第一组：排错后完成4轮

计时口径：“本轮用时”从消息就绪到 Codex 核对完成，包含准备、发送、网页等待和核对。网页等待从提交到观察到完整回复，包含检查间隔，不是纯模型生成时间。下表单位为秒。

| 序号 | 轮次 | 本轮用时 | 页面核验到提交 | 提交到完整回复 | Codex核对 |
|---|---|---:|---:|---:|---:|
| 1 | 方案第1轮 | 572.203 | 41.209 | 42.162 | 42.478 |
| 2 | 方案第2轮 | 148.915 | 33.654 | 46.884 | 33.247 |
| 3 | 方案第3轮 | 150.321 | 25.523 | 29.771 | 59.217 |
| 4 | 方案第4轮 | 118.217 | 23.588 | 28.290 | 31.313 |
| 5 | 最终复核（另计） | 194.952 | 28.739 | 115.612 | 18.070 |

1. 第1轮包含新任务错误继承恢复次数后的现场修复，不能代表正常单轮速度。
2. 第1、2轮讨论方案和边界，第3轮提出未知发送、崩溃窗口问题，第4轮确认稳定本地键、消息存在时补回执、未知时暂停；不承诺服务端恰好发送一次。
3. 方案就绪到最终方案共识共 **16分35.149秒**。实际执行至检查完成 **9分2.377秒**，最终复核阶段 **3分43.991秒**。从任务开始到DONE总计 **29分22.091秒**，包含排错及工具处理。
4. 页面刷新后发生过浏览器选择器超时，重新打开同一本地测试页面后操作通过；不能说全程没有故障。
5. 最终复核曾在07:32:33.507 UTC首次读到完整回复，但保存时证据过期，被正确拒绝；07:34:06.129 UTC重新读取后接受。表中115.612秒是检查点里可接受的观察时间，不倒填较早时间。真实首次读取距提交为22.990秒。

## 第二组：修复后连续3轮

| 序号 | 轮次 | 本轮用时 | 页面核验到提交 | 提交到完整回复 | Codex核对 |
|---|---|---:|---:|---:|---:|
| 1 | 方案第1轮 | 109.413 | 22.309 | 25.945 | 36.053 |
| 2 | 方案第2轮 | 92.490 | 19.390 | 30.087 | 24.098 |
| 3 | 方案第3轮 | 102.033 | 21.155 | 28.238 | 24.577 |
| 4 | 最终复核（另计） | 344.063 | 22.174 | 19.466 | 44.921 |

1. 新Task复用原官方对话，未现场修改核心代码。三轮依次核对总体方案、时间边界和验收；提前同意后继续专项检查，没有编造分歧。
2. 方案就绪到三轮共识共 **5分8.329秒**；从第一轮实际提交到三轮共识为 **4分20.914秒**。
3. 实际执行至检查完成 **1分22.866秒**，最终复核阶段 **5分46.171秒**，整个任务到DONE共 **12分17.935秒**。
4. 最终复核消息就绪后到发送用了4分39.676秒，包含 Codex 上下文恢复及读取准备；其中页面核验后到发送22.174秒。网页等待仅19.466秒，不能把整个最终复核耗时算给 DeepSeek。
5. 三个方案轮记录的最大检查延迟分别20.908、25.064、23.195秒；严格5秒目标未通过。使用规则约束和计时能暴露延迟，但不能消除宿主跨工具调度时间。

## 证据与复现

1. 本地第一组目录：工作区的 work/live1191-clean，包含 final-result.json、events.jsonl、基线、全量/专项测试日志和独立项目。
2. 本地第二组目录：工作区的 work/live1192-speed，包含 final-result.json、events.jsonl、基线、逐轮短消息与回执、23项测试日志和独立项目。
3. 公开计时摘要见 [1.19.2-timing.json](1.19.2-timing.json)，由两份真实 final-result.json提取。没有发布浏览器凭据、租约、私人对话地址或业务源码。
4. 自动化复现：在 core 运行 pnpm test、pnpm typecheck、pnpm build。新增回归位于 core/tests/review-reliability.test.ts；浏览器证据是合成输入，不能代替真实发送证明。
5. 两组最终状态都是DONE，最终复核均为当前任务的完整回复。后续真实任务仍需核对当前网页，不可复用这次测试回执取得执行权限。

## 更新后的使用

> 使用 Codex with ChatGPT，让 DeepSeek 多轮评审。每轮显示轮次，双方确认方案后自动修改、测试和检查页面，最后复核结果。原标签关闭先找回原对话，确实找不回时允许新建并带上已确认摘要继续。

本机更新会保留上一版备份。旧任务需要重新读取当前 Skill 和运行规则，历史消息不改写；不能声称安装文件更新就已自动刷新所有运行中任务。
