# 1.19.7 检查记录

## 本次修复

1. **最小身份释放/续租**：`ReleaseBrowserLease` 与 `RenewBrowserLease` 允许省略浏览器 runtime 身份，省略时按锁自身记录自校验；口令（token）与序号（epoch）仍然必须一致，缺失时给出明确中文原因，不再伪装成“身份不一致”。
2. **面板与真实任务分叉**：新增 `panelDrift` 识别“面板指向旧任务、绑定已指向安全活跃任务”的情况，返回真实任务号与对齐命令；新增 `verifiedAlignment` 逐项核验绑定（单一绑定、bound、归属、发送归属、无未确认回执/审计风险、任务文件同线程同方式、已激活、活跃、未取消、会话地址、会话/tab/runtime 三身份、runtimeEpoch）；新增 `c2c review bind` 显式对齐命令，且不接受操作者指定别的任务号。
3. **对齐是 fail-closed**：`alignReview` 把旧面板条目归档到 `history/`，新条目从 `PREPARING` 开始，共识/回复/回执/执行许可全部归零，必须重走门槛；`changeReview` 的 `allowTaskSwitch` 仅用于这一条已验证路径。
4. **失败原因透出**：`send_review_round.ps1` 新增 `Safe-NativeReason`，只接受脚本自己写的中文短原因（以句号结尾、长度不超过 120、不含路径/引号/括号/管道符/盘符），其余一律回落到通用文案。

## 已完成的本机检查

1. 类型检查：通过（0 错误）。
2. 全量测试：27 个文件、504 项通过（含新增 10 项回归）。
3. 构建：通过（`npm run build`）。
4. PowerShell 脚本解析：通过（根脚本和两份 DeepSeek 配套脚本）。
5. Git Bash 脚本语法：通过（6 份发布脚本）；两份 DeepSeek Skill 版本一致性：由 `build-review-manifest.mjs` 重新生成后校验为 1.19.7。

## 未做的事

1. 未修改业务项目文件。
2. 未改动 Codex 数据库、`state_5.sqlite` 或历史消息。
3. 对齐命令本身不发消息、不改业务文件；真实多轮复测在代码与文档同步后单独执行。

## 发布边界

全量测试、构建、脚本检查和右侧栏页面核对未通过前，不报告为“全部正常”，也不推送远端。
