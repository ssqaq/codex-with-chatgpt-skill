# 1.19.3 真实复测记录

## 验证结论

1. 修复后重新启动独立的3轮评审组。整体方案、计时边界、恢复与验收三轮都得到真实DeepSeek CONSENSUS；Codex核对全部专项后才整体确认。前两轮通过仅记专项同意，没有编造分歧，也没有提前执行。
2. execute前基线哈希相同；execute后确实修改独立副本，运行33项测试，打开页面检查，再发送结果摘要得到独立最终复核。最终状态DONE。不是四次“收到”，也不是仅写执行状态。
3. 本组发送前核验到提交及实际检查间隔全部不超过30秒，未出现错轮、重复发送、证据过期、冻结或再次修代码的情况。
4. 本地核心24个测试文件、412项通过；类型检查、构建通过；34份PowerShell脚本及6份Bash脚本语法通过。测试代码随仓库发布。

## 修复后新一组的实际时间

单位：秒。“网页等待”是提交到实际观察到完整正文，含检查间隔，不是网页标注的思考时间。“整轮”是摘要准备好到Codex核对完成，含工具调度和状态保存。

| 序号 | 阶段 | 核验到提交 | 网页等待 | 最大实际检查间隔 | 整轮 |
|---|---|---:|---:|---:|---:|
| 1 | 方案第1轮 | 29.030 | 24.920 | 24.796 | 136.865 |
| 2 | 方案第2轮 | 19.492 | 29.003 | 28.980 | 114.160 |
| 3 | 方案第3轮 | 18.569 | 23.468 | 23.432 | 89.160 |
| 4 | 最终复核（另计） | 23.128 | 22.547 | 22.518 | 88.995 |

1. 三轮方案：从第1轮摘要就绪到第3轮双方确认，共344.094秒，约5分44秒。
2. 实际修改、测试与页面：115.403秒；单独最终复核91.018秒。整组包含初始化衔接共551.075秒，约9分11秒。
3. [原始时间摘要](1.19.3-timing.json)保留毫秒，不用取整掩盖超时。两次检查之间含本地保存和工具调度时间。本组处于30秒目标内，不承诺未来每次都能固定耗时。
4. 三轮虽未卡住，整理和核对仍占时间；以上总时长包括人工代理和公开工具往返，不等于DeepSeek持续生成了5分钟。连续读取示例在本组调用时第一查已完整，提前返回；内部5秒循环、20秒上限和慢调用路径由自动化测试覆盖，不冒称该循环在真实网页中运行了多次。

## 前一组发现的问题与处理

1. 前一组做了4轮方案和1轮最终复核。首轮首次检查间隔33.213秒，比30秒多3.213秒；读到正文后没有及时存证，超过60秒被核心拒绝，随后真实重读。首次完整正文等待33.252秒，重读被接纳的等待127.192秒，两个时间分别保留，不能倒填。
2. 后三轮首次完整正文等待25.862、26.645、22.598秒。期间有排错和补充方案，整组不能称全程速度通过。首轮专项同意曾被测试编排提前记为整体执行共识；已纠正测试编排并说明专项尚未完成时继续待敲定，没有放宽正式执行门槛。
3. 最终复核前原浏览器标签关闭，重新打开原官方对话时被任务级一次恢复限制阻挡。新增真实关闭标签证据分支；原消息已确认、无待回执风险后成功找回同一对话。用户进一步明确同任务可恢复多次，已实现新的故障可再恢复、再次关闭的标签可用新证据找回。
4. 新增有界读取、部分失败保留、流式不完整标识不误停、先存证后分析、首轮短摘要优先。修改后才运行上面的全新3轮组，前一组不能替代修复后的复测。

## 页面与自动化覆盖范围

1. 两个独立副本各33项测试通过：原23项加10项倒计时边界。真实页面核对30秒显示、暂停后刷新仍暂停、恢复沿用原lastObservedAt且到期立即提示、完成与取消停止、新轮清空。
2. 核心读取器测试覆盖完整即返回、内部循环到20秒停止、部分成功后失败、首读失败、错轮、流式未完成标识、底层慢调用。它不发送消息，不写共识；底层调用由宿主控制，可能超过20秒，返回真实耗时，不伪装强制取消。
3. 实际PowerShell入口用合成浏览器证据验证同任务两次断开恢复、两次关闭标签恢复、过期/未来时间、错任务、旧标签还在、重复标签、未知回执、风险状态与旧证据重放；这些不是全部真实网页断网测试。
4. 新组未制造中断，保持同一官方对话、同一标签和两个开启的开关。前一组已验证一次真实关闭标签恢复；本次未删除官方对话来制造找不回后的新建场景。
5. 倒计时页面是独立验收项目，不是擅自修改Codex客户端的内置UI。发布的Skill仍按轮次和每满一分钟回显；本次未承诺宿主不运行时仍能计时回显。
